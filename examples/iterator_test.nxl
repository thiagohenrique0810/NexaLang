mod std;
use std::vec::Vec;
use std::option::Option;

fn main() -> i32 {
    let mut v: Vec<i32> = Vec::new();
    v.push(10);
    v.push(20);
    v.push(30);
    
    print("Iterating over Vec:");
    for x in v.iter() {
        print(x);
    }
    
    return 0;
}
