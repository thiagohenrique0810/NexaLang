# NexaLang VSCode Extension (local)

This is a minimal **syntax highlighting** extension for NexaLang files (`.nxl`).

## Install (local / dev)

1. Open VSCode
2. `Ctrl+Shift+P` → **Developer: Install Extension from Location...**
3. Select the folder `vscode-nexalang`

Alternatively:
1. Open the folder `vscode-nexalang` in VSCode
2. Press `F5` to run an Extension Development Host


