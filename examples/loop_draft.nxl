# Nested bindings do not change the outer loop counter.
fn main() -> i32 {
    let mut i: i32 = 5;
    while (i > 0) {
        let next: i32 = i - 1;
        print(i);
        i = next;
    }
    return 0;
}
