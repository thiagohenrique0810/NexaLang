kernel fn fill(buf: Buffer<i32>) {
    let i: i32 = gpu::global_id()
    if (i < buf.len) {
        let p: *i32 = buf.ptr
        p[i] = i
    }
}

fn main() {
    print("Vulkan SPIR-V kernels-only build")
}


