
struct Token { kind: i32, start: i32, len: i32 }

fn push_token(tokens: *Token, count: *i32, kind: i32, start: i32, l: i32) -> i32 {
    let idx: i32 = *count;
    *count = idx + 1
    return 0
}

fn main() -> i32 {
    return 0
}
