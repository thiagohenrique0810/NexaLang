struct Counter {
    value: i32
}

impl Counter {
    fn get(&self) -> i32 {
        let x: i32 = self.value;
        return x;
    }
}

fn main() -> i32 {
    let c: Counter = Counter(42);
    let val: i32 = c.get();
    print(val);
    return 0;
}
