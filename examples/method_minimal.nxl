struct Counter {
    value: i32
}

impl Counter {
    fn get(&self) -> i32 {
        return self.value;
    }
}

fn make_counter() -> Counter {
    let c: Counter = Counter(5);
    return c;
}

fn main() -> i32 {
    let c: Counter = make_counter();
    let val: i32 = c.get();
    print(val);
    return 0;
}
