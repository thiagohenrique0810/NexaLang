pub enum Option<T> {
    Some(T),
    None
}

impl<T> Option<T> {
    fn unwrap(self) -> T {
        let s = self;
        match (s) {
            Some(v) => return v,
            None => {
                 panic("Option::unwrap on None");
            }
        }
    }
}
